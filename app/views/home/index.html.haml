%h1 Welcome to Seeker Project!
%br
= form_tag search_path, method: :get do
  = text_field_tag :s
  = submit_tag 'Search documents'

%br
%br
- if user_signed_in?
  = link_to 'Manage Documents', documents_path
- else
  = link_to 'Login', new_user_session_path
